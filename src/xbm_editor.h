#ifndef __obtheme_xbm_editor_h
#define __obtheme_xbm_editor_h

/* Opens (or raises, if already open) the button-icon (XBM) editor
   window for the currently-open theme (main.h's current_doc). No-op
   if no theme is open. */
void xbm_editor_open(void);

#endif
